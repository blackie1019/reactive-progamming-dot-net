using System;

namespace app {
    public sealed class CalculateService {
        // private static readonly Lazy<CalculateService> lazy =
        //     new Lazy<CalculateService> (() => new CalculateService ());

        // public static CalculateService Instance { get { return lazy.value; } }

        // private CalculateService () {

        // }

        // public void Sum () {

        // }
    }
}